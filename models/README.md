# Models

To populate this folder with models available, please run [this script](scripts/download_models.py).

If you have a model that you would like to share, please add it to the `scripts/download_models.py` script so it gets downloaded.  
